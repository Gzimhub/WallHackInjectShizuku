workflows:
  build:
    name: Build APK
    environment:
      vars:
        ANDROID_SDK_ROOT: /opt/android-sdk
    scripts:
      - name: Build APK
        script: ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
